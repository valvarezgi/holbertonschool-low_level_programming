#include "holberton.h"
/**
 * add - is a function that adds two integers
 * and returns the result
 * @n: is a number
 * @m: is a number
 * Return: add between n and m
 */

int add(int n, int m)
{
	int add;

	add = n + m;
	return (add);
}
