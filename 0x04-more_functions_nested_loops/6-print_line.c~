#include "holberton.h"

/**
 * more_numbers - function that prints 10 times the numbers, from 0 to 14
 *
 */

void print_line(int n)
{ 
  int s; 
  
  if(n <= 0)
    {
      _putchar('\n');
    }
  else 
    {
      _putchar(

       
