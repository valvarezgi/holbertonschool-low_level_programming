#include "holberton.h"

/**
 * print_numbers - function that prints the numbers, from 0 to 9
 *
 * Return: 0
 */

void more_numbers(void)
{
  int i;

  do (i = 0; i < 15; i++)

    _putchar(i + '0');
  _putchar('\n');

  while (i = 10);
}
